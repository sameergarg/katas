/**
 * Checks if email address is valid or not. Valid email address is starting with at least one alphabet or number
 * followed by @ followed by at least one alphabet or number followed by . and then at least 2 to 4 alphabets
 */
package question.validation;
